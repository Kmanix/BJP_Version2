#include <iostream>

#include "Casino.h"
#include "GameMaterials.h"
#include <ctime>
#include <random>

int main()
{
	srand((unsigned int)time(NULL));

	Casino myCasino;
	/*
	RandomPlayer* p = new RandomPlayer(0, 1);

	p->addCard(Card(5, Hearts));
	p->addCard(Card(6, Hearts));
	p->addCard(Card(1, Hearts));
	p->addCard(Card(1, Diamonds));
	p->addCard(Card(1, Spades));
	p->addCard(Card(1, Clubs));

	cout << p->GetHandscore() << endl;
	*/
}
